{
  "name": "tim",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n chat,realtime-server -c blue.bold,green.bold \"yarn workspace react-server-components dev\" \"yarn workspace tim-chat-server watch\"",
    "setup:env": "node scripts/setup-contributor-env.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
