export enum Routes {
  // App
  Home = "/",
  TopicsForCircle = "/circles/:id",
  Topic = "/topics/:id",
  SignIn = "/signin",
  Terms = "/static/terms",
  Privacy = "/static/privacy",

  // API
  ChatHooks = "/api/chat-hooks",
}
